define(['routers/index'], function (PaymentUiIndexRouter) {
  describe('PaymentUiIndexRouter', function () {
    it('exists', function () {
      expect(PaymentUiIndexRouter).to.be.ok;
    });
  });
});