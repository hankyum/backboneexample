define(['routers/configuration'], function (ConfigurationRouter) {
  describe('ConfigurationRouter', function () {
    it('exists', function () {
      expect(ConfigurationRouter).to.be.ok;
    });
  });
});